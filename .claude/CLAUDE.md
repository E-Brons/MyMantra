# myMantra — Claude Code Instructions

Agent instructions, skills, and project conventions are in `.agents/`:

- `.agents/INSTRUCTIONS.md` — general conventions and skill index
- `.agents/skills/` — per-skill workflow files (read the relevant skill before starting a task)
