# MyMantra - Folder Structure

**Version:** 1.0
**Date:** March 2026
**Status:** Active
**Application Name:** MyMantra

This document describes the complete folder structure for the MyMantra Flutter application.

---

## Root Structure

```
myMantra/
├── android/                    # Android native code (auto-generated by Flutter)
├── assets/
│   ├── audio/                  # Notification sounds (singing bowl, temple bell, etc.)
│   │   └── notification-sounds/
│   ├── fonts/                  # Script fonts downloaded by make install; gitignored
│   ├── images/                 # Illustrations, background images, icons
│   └── localization/           # Localization files (deferred — Phase 2.0+)
├── docs/
│   ├── README.md               # Documentation index
│   ├── product/
│   │   ├── application_goals.md
│   │   ├── app_flowchart.md
│   │   ├── builtin_mantras_library.md
│   │   └── product_requirements.md
│   └── software/
│       ├── build-system-architecture.md
│       ├── folder_structure.md
│       ├── phased_implementation.md
│       ├── software_architecture.md
│       └── software_requirements.md
├── ios/                        # iOS native code (auto-generated by Flutter)
├── lib/
│   ├── main.dart               # App entry point (ProviderScope + MyMantraApp)
│   └── src/
│       ├── app/
│       │   ├── app.dart        # Root app widget
│       │   ├── router.dart     # go_router configuration and route definitions
│       │   └── theme/
│       │       ├── app_colors.dart     # Color palette constants
│       │       └── app_theme.dart      # ThemeData configuration
│       ├── core/
│       │   ├── models/         # Shared domain models (Mantra, Session, Settings, …)
│       │   ├── providers/      # Top-level Riverpod providers
│       │   ├── services/       # HapticService, StorageService
│       │   └── utils/          # Date/timezone utilities and streak logic
│       ├── features/
│       │   ├── library/        # Built-in mantra library
│       │   ├── mantras/        # User mantra management
│       │   ├── progress/       # Streak and stats tracking
│       │   ├── session/        # Session experience (repetition counter, timer)
│       │   └── settings/       # User settings
│       └── shared/
│           └── widgets/        # Cross-feature widgets (AppScaffold, …)
├── make/                       # Build system scripts (called by Makefile)
│   ├── lib.sh                  # Shared variables and target.json parsing helpers
│   ├── prerequisites.sh        # Verify only: checks tools and arch; writes prerequisites.yaml
│   ├── prerequisites.yaml      # Gate file written on success; gitignored
│   ├── install.sh              # Installs toolchain per active targets; calls prerequisites.sh
│   ├── build.sh                # Compiles release artifacts for active targets
│   ├── run.sh                  # Launches app in run or debug mode
│   └── clean.sh                # Removes all build artifacts per target
├── macos/                      # macOS native code (auto-generated by Flutter)
├── test/                       # Unit, widget, and integration tests (mirrors lib/src/)
├── web/                        # Web platform files (auto-generated by Flutter)
├── .vscode/                    # VS Code workspace config (committed)
│   ├── tasks.json              # make install / build / run / debug / clean tasks
│   ├── launch.json             # Flutter debug launch configs per target
│   ├── settings.json           # Workspace settings and file exclusions
│   ├── extensions.json         # Recommended extensions
│   └── mymantra.code-snippets  # Project-specific snippets
├── Makefile                    # Thin dispatcher: install / build / run / debug / clean
├── pubspec.yaml                # Flutter dependencies and metadata
└── target.json                 # Active build targets and per-target config (committed)
```

> Platform directories (`android/`, `ios/`, `macos/`, `web/`)  are committed as required by Flutter.
> Build artifacts within them are gitignored and hidden in VS Code

---

## Source Code Structure (`lib/src/`)

Each feature is a self-contained module. Cross-feature dependencies go through `shared/` or `core/`.

```
lib/src/
├── app/
│   ├── app.dart                        # Root app widget (MaterialApp / ProviderScope)
│   ├── router.dart                     # go_router configuration and route definitions
│   └── theme/
│       ├── app_colors.dart             # Color palette constants
│       └── app_theme.dart              # ThemeData configuration (light + dark)
│
├── core/
│   ├── models/                         # Shared domain models
│   │   ├── achievement.dart
│   │   ├── mantra.dart
│   │   ├── progress.dart
│   │   ├── session.dart
│   │   └── settings.dart
│   ├── providers/
│   │   └── app_provider.dart           # Top-level Riverpod NotifierProvider
│   ├── services/
│   │   ├── haptic_service.dart         # HapticFeedback wrapper
│   │   └── storage_service.dart        # shared_preferences wrapper
│   └── utils/
│       └── date_utils.dart             # Date/timezone utilities and streak logic
│
├── features/
│   ├── library/                        # Built-in mantra library
│   │   ├── data/
│   │   │   └── built_in_mantras.dart
│   │   └── screens/
│   │       └── library_screen.dart
│   ├── mantras/                        # User mantra management
│   │   └── screens/
│   │       ├── home_screen.dart
│   │       ├── create_mantra_screen.dart
│   │       └── mantra_detail_screen.dart
│   ├── progress/                       # Streak and stats
│   │   └── screens/
│   │       └── progress_screen.dart
│   ├── session/                        # Repetition counter and timer
│   │   └── screens/
│   │       └── session_screen.dart
│   └── settings/                       # User preferences
│       └── screens/
│           └── settings_screen.dart
│
└── shared/
    └── widgets/
        └── app_scaffold.dart           # Common scaffold with bottom navigation
```

---

## Test Structure

```
test/
├── core/
│   ├── models/                 # Tests for shared domain models
│   ├── services/               # Tests for notification, haptic, storage services
│   └── utils/                  # Tests for date/timezone utilities
├── features/
│   ├── library/
│   ├── mantras/
│   ├── session/
│   ├── progress/
│   └── settings/
└── helpers/                    # Shared test utilities and mocks
```

---

## Key Principles

### 1. Clean Architecture
- **Domain layer** (`core/models/`, `features/*/domain/`): Pure Dart entities, repository interfaces, use cases — zero framework dependencies
- **Data layer** (`features/*/data/`): Data sources and repository implementations
- **Presentation layer** (`features/*/screens/`, `features/*/providers/`): Riverpod providers, screens, widgets

### 2. Feature-Based Organization
- Each feature is a self-contained module under `lib/src/features/`
- Cross-feature dependencies go through `lib/src/shared/` or `lib/src/core/`
- Facilitates independent testing and future team scaling

### 3. `lib/src/` Convention
- `lib/main.dart` — entry point only
- `lib/src/` — all implementation; signals internal code to contributors
- Follows the Dart package `src/` convention used by the core SDK

### 4. State Management (Riverpod)
- All state lives in Riverpod `providers/`
- Screens consume providers via `ConsumerWidget` or `ConsumerStatefulWidget`
- No global mutable state

### 5. Theming
- All theme configuration in `lib/src/app/theme/`
- Light and dark themes fully defined
- No hardcoded colors or text styles in widgets

### 6. Assets at Root
- Static resources separate from source code
- Clear separation of code vs. media

---

## File Naming Conventions

### Dart Files
- **snake_case**: `mantra_card.dart`, `session_screen.dart`, `create_mantra.dart`
- **Suffix guide**:
  - `_screen.dart` — full-screen widgets
  - `_widget.dart` — complex reusable widgets
  - `_provider.dart` — Riverpod providers
  - `_model.dart` — Isar-annotated data models
  - `_entity.dart` — Domain entities (or just the name: `mantra.dart`)
  - `_repository.dart` — Repository interfaces and implementations
  - `_usecase.dart` — Use case classes

### Classes
- **PascalCase**: `MantraCard`, `SessionScreen`, `MantraRepository`, `CreateMantra`

### Tests
- **Match source file + `_test`**: `mantra_card_test.dart`, `session_screen_test.dart`
- Located in `test/` mirroring the `lib/src/` structure
